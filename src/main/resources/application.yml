server:
  port: 8080

spring:
  application:
    name:classdemo
  datasource:
    username: 账号
    password: 密码
    url: jdbc:mysql://localhost:3306/数据库名字?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html
mybatis:
  mapper-locations: classpath*:mapping/*.xml
  type-aliases-package: com.nsw.dirs
cbs:
  imagesPath: D:/zhaopian/
